[package]
name = "ringo"
version = "0.1.0"
edition = "2024"

[dependencies]
clingo-sys = { workspace = true }
ringo-derive = { path = "../ringo-derive" }

[features]
default = ["static-linking"]
static-linking = ["clingo-sys/static-linking"]
