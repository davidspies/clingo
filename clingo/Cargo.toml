[package]
name = "clingo"
version = "0.1.0"
edition = "2024"

[dependencies]
clingo-sys = { workspace = true }
clingo-derive = { path = "../clingo-derive" }

[features]
default = ["static-linking"]
static-linking = ["clingo-sys/static-linking"]
